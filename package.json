{
  "name": "codex-loop",
  "version": "0.2.0",
  "private": true,
  "description": "A Codex plugin for adaptive and Cron loops in the current conversation.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check plugins/codex-loop/scripts/*.mjs plugins/codex-loop/scripts/lib/*.mjs test/*.mjs",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
